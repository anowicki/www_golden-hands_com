<VirtualHost *:80>
    SetEnv          ee_env    "prod"
    SetEnv          ee_debug  "false"

    ServerName     www.golden-hands.pl
    ServerAlias    xn--zote-rce-seb81b.pl www.xn--zote-rce-seb81b.pl golden-hands.pl
    DocumentRoot   /var/www/sites/www_golden-hands_pl/web
    DirectoryIndex app.php

    ErrorLog  /var/log/httpd/www_golden-hands_pl.error
    CustomLog /var/log/httpd/www_golden-hands_pl.access common

    AddDefaultCharset UTF-8

    <Directory /var/www/sites/www_golden-hands_pl/web/>
        AllowOverride All
        Allow from All
        Require all granted

        RewriteEngine On
        # Always redirect to WWW
        RewriteCond %{HTTP_HOST} ^www\.golden-hands\.pl$ [NC]
        RewriteRule ^(.*)$ http://golden-hands.pl/$1 [R=301,L]

        RewriteCond %{HTTP_HOST} ^www\.xn--zote-rce-seb81b.pl$ [NC]
        RewriteRule ^(.*)$ http://xn--zote-rce-seb81b.pl$1 [R=301,L]

        # Symfony stuff
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteRule ^(.*)$ app.php [QSA,L]
    </Directory>

</VirtualHost>
